import React from "react";

const SidebarDivider = ({ name }) => {
  return (
    <div className="sidebar-nav-item divider mb-10">
      <p>{name}</p>
    </div>
  );
};

export default SidebarDivider;
